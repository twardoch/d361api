# This file is maintained for backwards compatibility
# The main CI/CD is now handled by push.yml and release.yml
name: Legacy Python Build

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy Notice
        run: |
          echo "This workflow is deprecated."
          echo "Please use 'Build & Test' workflow (push.yml) instead."
          echo "For releases, use the 'Release' workflow (release.yml)."